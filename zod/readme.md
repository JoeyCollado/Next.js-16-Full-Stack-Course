# https://zod.dev/
# a typescript first schema validation library with static type inference
# with this we will create a schema 
# here we will apply it to define our three inputs (name, email, password) rules and the rules will be taken to react-hook-form