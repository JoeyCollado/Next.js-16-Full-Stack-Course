# 1. create a schema
# 2. initialize react hook form
# 3. 

# server components or client components

# use server components for server functionalities like fetch data from databases or api's close to the source, reduce the amount of javascript sent to browser, 

# if need interactivity like state, event handlers, lifecycle logic use client components.